{
  "scripts": {
    "dev": "npm run dev --prefix portfolio",
    "build": "npm install --prefix portfolio && npm run build --prefix portfolio",
    "preview": "npm run preview --prefix portfolio"
  },
  "dependencies": {
    "react-icons": "^5.7.0",
    "tailwind": "^4.0.0"
  }
}
